# ==========================================
# Distilled Embedding Engine Requirements
# ==========================================

# [Backend Selection: Choose ONE]
# --------------------------------
# Option A: PyTorch/CUDA Backend (For NVIDIA Linux Clusters & MPS Fallback)
torch>=2.0.0
safetensors

# Option B: Apple Silicon MLX Backend (For ultra-fast Local Mac pure development)
# Uncomment the line below and comment out torch if strictly using the MLX framework
# mlx>=0.14.0

# Data Processing
numpy
pandas
pyarrow

# ==========================================
# GodCore Infrastructure External Dependency
# ==========================================
# In production, the training core is expected to be an external package install:
git+https://github.com/catclever/Huntun.git
#
# For local development with the Submodule topology, install the local path in editable mode:
# -e ../training/core
